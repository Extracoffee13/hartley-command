# Industry Pulse — AI / Agent Ecosystem · 2026-08-28

Daily scan for The Construct. Source URL · one-line summary · applicable to The Construct.

1. **Claude Code 2.1.248 shipped `--restricted` — a mode that REMOVES the command-running tools instead of asking permission for them, which is the first STRUCTURAL answer to the credentialed-unattended-agent problem this report has tracked for three weeks** ([raw.githubusercontent.com/anthropics/claude-code](https://raw.githubusercontent.com/anthropics/claude-code/refs/heads/main/CHANGELOG.md)) — Verified against the canonical CHANGELOG.md, not a summary of it: `--restricted` (or `CLAUDE_CODE_RESTRICTED=1`) removes the built-in tools that run commands or code and removes `WebFetch` unless it is named in `--tools`, keeps file tools inside the working directory, refuses `bypassPermissions`, and ignores user, project and local settings files. Two further 2.1.248 entries land on the same automation surface: `claude self-hosted-runner --client-label <label>` overrides the label the runner registers with, and server-managed settings diagnostics now emit a startup warning plus a `/doctor` and `/status` line when settings fail to load or were never fetched — the failure reasons named explicitly are Bedrock/Vertex/third-party provider and a custom `ANTHROPIC_BASE_URL`. `/usage-credits` arrives for Enterprise orgs billed through AWS Marketplace, and cross-session `SendMessage`/`ListAgents` now works on Bedrock, Vertex and Foundry and with telemetry disabled. 2.1.250 is the newest entry on the changelog and is bug fixes only. **Applicable: YES** — YES, and it is the most useful item this report has surfaced for the Hermes design in a month. `2.1.248`, `2.1.250`, `CLAUDE_CODE_RESTRICTED`, `cacheTtl` and `usage-credits` all return ZERO matches across the corpus; Releasebot's newest entry is still 2.1.247, so this was read off the vendor's own file rather than a mirror — which is exactly the discipline yesterday's scan wrote down. The reason it matters is specific. Every permission item this report has logged since 08-07 — the 2.1.223 evaluated-string bypass, the dangling `&&`/`||` approval fix, the ANTHROPIC_BASE_URL telemetry leak — has been a patch narrowing the gap between what the permission system checks and what the shell runs. `--restricted` does not narrow that gap; it deletes the surface. For a scheduled loop that only reads the public web and writes files into one repo — which describes this Pulse scan, the Lens sweep, the answer-page checks and most of the daily fleet — the shell tools are not a capability that needs guarding, they are dead weight with a blast radius. The clause that makes it load-bearing rather than cosmetic is "ignores user, project and local settings files": a mode that cannot be widened by editing a settings file is a different security object from a mode that merely defaults to narrow, and that distinction is the whole argument for using it on unattended jobs. Two honest caveats. First, `--restricted` also removes `WebFetch` unless named in `--tools`, so a research loop must opt its fetch tool back in explicitly — this is a per-loop configuration change, not a global flag anyone should flip across the fleet in one pass. Second, nobody here has run it; the changelog states behaviour, it does not verify it in Bobby's build. The action is one scoped experiment on a single read-only loop, not a migration. The server-managed settings diagnostics are the small companion win: the fleet has repeatedly failed silently on config, and a `/doctor` line that names the reason converts a silent wrong result into a visible one.

2. **The SAME release quietly closed a credential EXFILTRATION path — `/ultrareview` and cloud sessions were uploading uncommitted `prod.env`-style files, `*.tfvars`, and editor swap and backup copies of keys like `key.pem.tmp` and `id_rsa.swo` — the second consecutive day of credential-scoping fixes** ([raw.githubusercontent.com/anthropics/claude-code](https://raw.githubusercontent.com/anthropics/claude-code/refs/heads/main/CHANGELOG.md)) — The 2.1.248 entry reads: fixed `/ultrareview` and locally seeded cloud sessions uploading uncommitted edits to `prod.env`-style and `*.tfvars` files, or to editor swap, temp, and backup copies of credential files (e.g. `key.pem.tmp`, `id_rsa.swo`); they now stay on the machine. Three more entries in the same version touch unattended and desktop work directly. Claude Desktop and Cowork sessions were disappearing after 30 days — transcript cleanup now keeps desktop-written sessions while they are in the app, with a new `desktopSessionCleanupPeriodDays` setting capping the exemption. A background session with a `PermissionRequest` or `PreToolUse` hook that prints an invalid answer used to wait silently; the `claude agents` row now names the hook and the schema error. And a backgrounded worktree session now holds the worktree's lock while it runs, so cleanup and `git worktree remove` leave it alone. On cost, `experimental.cacheTtl` (`"5m"` or `"1h"`) is now settable per agent in frontmatter, and the Workflow tool's description dropped from about 5.7k tokens to about 1k with the script-writing reference moved into a bundled `workflow-authoring` skill. **Applicable: YES** — YES, and the exfiltration fix is the one to actually check. `ultrareview`, `tfvars`, `workflow-authoring` and `desktopSessionCleanupPeriodDays` all return zero corpus matches. Yesterday's finding was that telemetry was sending a gateway's API key to the wrong host; today's is that a review command was sending uncommitted secret-adjacent files off the machine. Neither is a breach here and neither should be treated as one — but the pattern across two consecutive releases is that the leak path was a legitimate feature reading more of the working directory than its purpose required, which is precisely the failure class an environment full of SSH keys, deploy credentials and `.env` files is exposed to. The concrete check is small and bounded: has anyone run `/ultrareview` or seeded a cloud session from a directory that holds DreamHost or GitHub credentials, and is the fleet past 2.1.248. Fold it into the version-and-config check already carried and unresolved from the last two days — past 2.1.223, past 2.1.246, past 2.1.248, no wildcard before a Bash subcommand, and whether any machine sets `ANTHROPIC_BASE_URL`. That is now one task with four checkboxes, not four tasks, and it has been open long enough to be the most overdue item in this corpus. The Cowork 30-day fix is worth naming separately because it is invisible until it costs something: Bobby's daily loops are Cowork sessions, and a transcript quietly deleted at day 30 is history this report's own continuity depends on. The two cost items pair with yesterday's `ANTHROPIC_DEFAULT_MODEL` and the per-loop `/usage` panel — the fleet now has three independent cost levers logged in three days and has pulled none of them.

3. **CORRECTION — yesterday's falsification step KILLED a real feature: `SendFeedback` shipped in 2.1.247, and this report rejected it because a search summary had dressed it in a fabricated filesystem path** ([releasebot.io](https://releasebot.io/updates/anthropic/claude-code)) — Yesterday's bullet 4 recorded a kill: a search summary claimed that on August 27 Claude Code announced it now writes its own feedback reports, with drafts queued locally in `~/.claude/feedback/drafts/` until sent, and the scan rejected the whole item after finding nothing on the vendor's release surfaces. Read directly today, the 2.1.247 entry — first seen by Releasebot on Aug 27, and present in the canonical CHANGELOG — says: "Added the `SendFeedback` tool: when something goes wrong in a session, Claude can draft a feedback report for you to review and send from `/feedback` (turn off with the `feedbackDrafts` setting)." The capability is real, the date is right, and the mechanism is right. The only false element was the invented directory path. The rest of 2.1.247 was also missed and is worth naming: `/claude-api cost-optimize` profiles an existing project's API spend and works through caching, token hygiene, batch, effort and model choice one measured change at a time; terminal hyperlinks in rendered markdown that point at a network or automounter path, contain a control character, or lead with an invisible character now render as plain text; plugin marketplace names containing control or invisible characters are rejected; and on Bedrock, Vertex, Foundry and telemetry-disabled sessions Claude is now TOLD when a configured MCP server failed to connect instead of concluding its tools do not exist. **Applicable: YES** — YES, and this is the most valuable entry today because it corrects this report rather than the world. Yesterday's lesson — "a summary of a source is not the source" — was right and stays. What it missed is the failure mode on the other side: the summary was mostly TRUE, and the scan threw out a true claim because one specific attached to it was false. Falsification has to separate the claim from its ornament. The correct verdict yesterday was "feature confirmed, path unverified and probably invented," which is a shipped bullet with a caveat, not a kill. A report that only guards against false positives will accumulate false negatives instead, and a false negative is harder to catch precisely because nothing enters the corpus to later contradict. The concrete rule: when a source summary contains several claims, verify each independently and report the surviving subset — do not let one bad detail take the rest with it. On substance, two 2.1.247 items are directly applicable. The MCP connection-failure notice is the fix for a failure this environment produces constantly — this very session opened with sixteen servers needing authentication and several still connecting, and a model that silently concludes a tool does not exist will confidently report a capability gap that is really an auth gap. And the terminal-hyperlink hardening is prompt-injection defence in the exact shape this stack is exposed to: an agent that reads scraped web pages and renders markdown into a terminal.

4. **CORPUS GAP — general contractors' full project record is now agent-queryable over MCP: Constructable exposes drawings, RFIs, submittals, daily logs, change events, financials and quality lists to Claude and ChatGPT, aimed squarely at the $20M–$150M GC that is Brand 9's actual buyer** ([constructable.ai](https://constructable.ai/blog/construction-mcp-server-integration)) — Verified by reading the vendor's own page rather than a search summary. Constructable, a Y Combinator construction-management product built for mid-size general contractors running $20M to $150M in annual volume, ships a native remote MCP server at `https://api.constructable.ai/mcp` that exposes the core project record — drawings, RFIs, submittals, daily logs, change events, financials and quality lists — to any MCP-compatible client, with a listed ChatGPT App and step-by-step Claude custom-connector instructions. It offers write access as well as read, and the vendor's own setup guidance recommends setting read-only tools to "Always Allow" while leaving write and delete tools on "Needs Approval." Pricing is flat-fee with unlimited users, explicitly so external AI tools can draw on the same project data without per-seat cost. The post is dated July 6, 2026 and titled for June 2026 — this is a corpus gap, not today's news, and it is logged with that stated plainly. **Applicable: YES** — YES for Brand 9, and it is the first construction-side MCP this corpus has ever carried: `Constructable` returns zero matches across every file, while the adjacent verticals this report already logged — Green Street for commercial real estate, Rechat for residential, and the CastHub/Screenly/Revel Digital digital-signage servers — are all carried and suppressed today. The gap matters because those three were all one step away from Bobby's business, and this one is not. Brand 9 sells into homebuilders and general contractors; ATLAS exists because Lennar sign operations is a tracking problem across communities, permits, install crews and status. The objects Constructable exposes are the objects a sign scope actually lives inside: a submittal is where a monument sign spec gets approved, a change event is where a wayfinding revision becomes billable, and a daily log is where an install date is really recorded. Three reads, in descending confidence. First and most solid: this is evidence for the PRAGMA pitch, because a GC whose project record is already agent-queryable is a buyer who has stopped needing to be convinced that agents belong in construction — the education cost is gone, and the conversation starts at what to point the agent at. Second: the vendor's own read-allow / write-approve recommendation is the same permission posture this report has been arguing for all week, arrived at independently by a construction vendor, which is a small confirmation that the boundary convention is converging rather than being invented here. Third and most speculative, flagged as such: if a builder client ever runs Constructable, a sign vendor with read access to the submittal and change-event stream would know about a scope change before the email arrives — that is a real advantage and it is entirely hypothetical, since nobody has confirmed a single B9 client uses this product. The honest status is a capability to hold and a question to ask a builder contact, not a build.

5. **Frontier flat for the FIFTEENTH consecutive scan — and an anonymous "OX Alpha" model beating GPT-5.6 on coding benchmarks was refused for lack of attribution** ([llm-stats.com](https://llm-stats.com/llm-updates)) — No frontier lab — OpenAI, Google, Meta, Mistral, xAI, DeepSeek or Cohere — shipped a model or material availability change in this window. GLM-5.2 Turbo from Z.AI (Aug 17) remains the most recent entry; Gemini 3.7 Flash (Aug 13) is Google's newest and is already carried, and OpenAI's flagship line is still the July GPT-5.6 Sol/Luna/Terra tracks with Luna the free-tier default. One candidate was refused rather than killed: a tracker summary describes an "OX Alpha" model outperforming GPT-5.6 on coding benchmarks with production adoption inside 24 hours and a free preview ending around Aug 27, while stating that official attribution has not been confirmed and the creator remains anonymous. An unattributed frontier-class model is not a reportable release, and it is recorded here as seen-and-refused so a later scan does not rediscover it as new. Suppressed as already carried: the entire agent-funding slate (Instinct's $250M at $2.5B, HappyRobot's $150M at $1.2B, Zenity's $125M, Cognition at $40B-plus); every adjacent MCP item that resurfaced this window — X Ads' 23-tool server from 08-25, the CastHub/Screenly/Revel Digital signage servers, Green Street, Rechat, and the MCP 2026-07-28 stateless/OAuth/OIDC spec work; and arXiv 2607.08028 "From Prompts to Contracts: Harness Engineering for Auditable Enterprise LLM Agents," carried since 07-14. **Applicable: MAYBE** — MAYBE, and the refusal is the part worth keeping. "Beats GPT-5.6, anonymous creator, preview ending imminently" has the exact shape of a claim that is interesting enough to repeat and impossible to check, and the corpus already carries the cost of that mistake twice — the fabricated feedback path yesterday and the false signage-MCP negative that survived weeks. Refusing on attribution rather than on plausibility is the cheaper rule, because plausibility is what makes these items dangerous in the first place. On substance, fifteen flat scans is now more than two consecutive months in which the ecosystem's news has been permission modes, credential scoping, cost levers, governance gateways and vertical MCP coverage — not capability. Today makes that unusually clean: the single most consequential item in this scan is a flag that DELETES tools, and the second is a fix that stops a feature reading files it did not need. That is what a maturing platform's news looks like, and it is the entire PRAGMA argument stated by the market — when nobody can differentiate on model access, the defensible layer is proprietary data and governed delivery, which is what the AAIR score history, the answer-page corpus and the attribution chain are. The suppression list is itself a signal worth noting: four separate MCP stories resurfaced in this window and every one was already in the corpus, which means the thread is now running ahead of the coverage rather than behind it.

6. **CORRECTION TO THIS MORNING'S OWN BULLET 5 — the frontier was NOT flat: two models shipped on Aug 26 (Zhipu's GLM-5.3-Flash and Qwen3.8-Flash-Next), and the 06:04 fire's "fifteenth consecutive flat scan" rested on a most-recent-release date taken from a search summary of the tracker instead of the tracker** ([llm-stats.com](https://llm-stats.com/llm-updates)) — Read directly off the llm-stats release timeline rather than a search engine's description of it. The page's "AI Model Releases" list is stamped `Aug 26, 2026 - 2 releases`: Zhipu AI's GLM-5.3-Flash (open source, carried by both Novita and DeepInfra at $15.00/M input) and Alibaba/Qwen's Qwen3.8-Flash-Next, both labelled "2d ago". The 06:04 fire recorded that "GLM-5.2 Turbo from Z.AI (Aug 17) remains the most recent entry," and neither half of that survives contact with the source: the tracker's Zhipu organization card lists its recent models in the order GLM-5.3-Flash, GLM-5.3, GLM-5.2, and dates GLM-5.3 to Aug 14 — a date this corpus has carried since 08-14 — which puts GLM-5.2 BEFORE it, not after. The same page also dates Grok 4.6 to Aug 12, not the Aug 6 a search summary supplied earlier this month, and shows DeepSeek-V4-Flash-Vision-Exp on Aug 21, which is likewise unlogged here. One narrower claim does hold and is worth stating precisely: both Aug 26 models are speed/flash-tier variants, and no frontier lab — OpenAI, Google, Meta, Mistral, xAI, DeepSeek or Cohere — shipped a FLAGSHIP in this window. Note also that llm-stats now fronts the page with a "Confirm you're human" interstitial; the content rendered in full behind it, but that is a bot shield on a source this report reads daily and it will eventually bite. **Applicable: MAYBE** — MAYBE on substance, YES on method, and the method is the point. `GLM-5.3-Flash`, `Qwen3.8-Flash-Next`, `Flash-Next` and `V4 Flash Vision` all return zero matches across the 60 files in this corpus. Nothing here changes what Bobby should build: two flash-tier open-weight releases from Chinese labs do not alter the PRAGMA thesis, do not touch B9, and do not merit a model migration. What they do is falsify the FRAME. "Fifteenth consecutive flat scan" has been load-bearing rhetoric in this report for two weeks — it is the sentence the entire "nobody differentiates on model access anymore, so the defensible layer is proprietary data and governed delivery" argument rests on. That argument is probably still right, and it is now resting on a count that was maintained without checking. The correct restatement is narrower and survives: no FLAGSHIP frontier release in fifteen scans, while flash-tier and open-weight releases continue at roughly weekly cadence. That is a real and defensible observation about where the competition has moved — down-market, toward cheap fast inference — and it is arguably a BETTER version of the same argument, because commodity pricing at $15.00/M is the commoditization thesis stated in dollars rather than asserted. The process failure is the one to log. This morning's own PRAXIS lesson was "verify each claim in a summary independently," written after killing a real feature over a fabricated path. Eight hours later the same report applied that discipline to the Anthropic changelog, which it doubted, and skipped it for the frontier tracker, which it expected to be boring. A falsification step that runs only on interesting claims is not a falsification step.

7. **CORPUS GAP that dents a standing negative — PTC shipped an MCP server that lets Claude WRITE CAD AUTOMATION CODE rather than read CAD data: Onshape's FeatureScript MCP Server turns natural language into reusable parametric features, explicitly "text-to-code-to-CAD"** ([ptc.com](https://www.ptc.com/en/news/2026/onshape-launches-featurescript-mcp-server)) — Verified from PTC's own newsroom release, not a search summary. Dated August 13, 2026 out of Boston, PTC (NASDAQ: PTC) announced the FeatureScript MCP Server in its Onshape CAD and PDM platform, shipped through the Onshape Labs initiative and listed on the Onshape App Store. FeatureScript is Onshape's own programming language for CAD automation; the MCP server extends it to coding LLMs including Claude, ChatGPT and Gemini, so an engineer describes the functionality they need and the model helps build, test, debug and refine the custom feature until it works. The framing in the release is deliberate and is the interesting part. David Katzman, EVP and GM of Onshape and Arena at PTC: "The future of AI in CAD is not simply text-to-CAD, but text-to-code-to-CAD... The real value of AI is helping engineers turn proven ideas into tools they can use again and again." PTC lists as a stated benefit that the resulting tool sets are reusable "without repeated prompting, ongoing AI interactions, or recurring AI costs." IDC's Jeff Hojlo, quoted in the same release: "The significance of this announcement isn't that AI can generate another CAD model. It's that AI is being used to create engineering capability... the focus is shifting from generating outputs to capturing and operationalizing engineering expertise." Dated Aug 13 — logged as a corpus gap, not as today's news. **Applicable: YES** — YES for VironBox/STRUCTA, and it forces an honest amendment to a negative this report has repeated for weeks. `Onshape` and `FeatureScript` return zero matches across all 60 files; the apparent `PTC` hits in the corpus are false positives, the substring inside `sgcaptcha`. The standing negative, carried on 08-23, 08-24 and 08-25, reads "there is still no MCP server for signage, wayfinding, architectural fabrication or permitting." Signage, wayfinding and permitting remain genuinely uncovered — that part stands and should keep being checked. "Architectural fabrication" no longer reads clean, because parametric feature authoring over MCP is the adjacent capability, and it now exists in shipping mechanical CAD. Two honest limits before anyone acts. Onshape is mechanical CAD and PDM, not Revit or BIM: this does not touch the APS Design Automation path STRUCTA is built on, and there is no Revit equivalent announced here. And nobody at this shop uses Onshape, so the product itself is not a purchase. What transfers is the PATTERN, and the pattern is one this stack already runs without having named it as an industry convergence: do not make the model redo the work on every invocation — have it AUTHOR a durable artifact and keep the artifact. A FeatureScript feature is that. A MAXScript job dropped into `C:\HC\jobs` is that. A skill file is that. PTC's own stated economic argument for the design — reuse without recurring AI cost — is the same argument the skills library makes, arrived at independently by a $7,000-employee CAD vendor and stated in a press release. For PRAGMA that is the second engineering-tool vendor in a single day, after Constructable in this morning's bullet 4, shipping an agent surface into the built environment. The pitch line is no longer "agents are coming to construction"; it is "your CAD vendor and your GC's project system both already shipped this, and the question is what governs it."

8. **CORPUS GAP in Claude Code 2.1.236 — a macOS sandbox fix stopping a denied `**/.env` from being read by simply RENAMING the file, plus `notify_when_idle`, the first primitive for one agent session to wait on another without polling** ([releasebot.io](https://releasebot.io/updates/anthropic/claude-code)) — Read off the version entries directly rather than the digest paragraph at the top of the page, which turned out to be summarizing Week 34 rather than this week — the trap that produced today's first correction. Version 2.1.236, inside Week 34 (August 17-21, 2026), carries two entries this corpus never logged. First, the sandbox fix: on macOS, wildcard read-deny rules such as `**/.env` now take precedence inside allowed read regions, cover the contents of matched directories, and cannot be bypassed by renaming the denied file. Second, `notify_when_idle` was added to cross-session `SendMessage` — one Claude Code session can ask another session on the same machine to send a single notice when it next goes idle; opt-in, one-shot, no polling, macOS and Linux. The same version also made `/goal` check in automatically after 30 minutes, then at 1h and 2h, when it is parked behind long-running background work, with `CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` to opt out. This is an OLDER release than the 2.1.248 covered at 06:04 and is logged as a gap, not as news. **Applicable: YES** — YES, with the action stated carefully so it does not become busywork. `notify_when_idle`, `read-deny` and `2.1.236` all return zero corpus matches. The sandbox item is the third credential-scoping entry this report has logged in three days, and the escalation across them is worth naming: Wednesday's was telemetry sending a gateway key to the wrong host, this morning's was `/ultrareview` uploading uncommitted `prod.env`-style files, and this one is a DENY RULE that could be defeated by renaming the file it protected. The first two were features reading more than they needed; this one is a control that did not hold. In an environment carrying DreamHost SSH keys, GitHub credentials and `.env` files, that is the more serious category. The practical consequence is small and should be said plainly rather than dressed up: 2.1.236 is far behind 2.1.248, so any machine that passes the version check already carried and unresolved for three days is covered by definition. This does not add a task, it adds a floor — and it is one more reason the check itself is now the most overdue item in this corpus: past 2.1.223, past 2.1.246, past 2.1.248, no wildcard before a Bash subcommand, and whether any machine sets `ANTHROPIC_BASE_URL`. One task, four checkboxes, four days open. `notify_when_idle` is the item with genuine design value here, because it addresses a problem this stack has repeatedly solved by building infrastructure around it. The `b9-site-repair-headless` skill exists because concurrent Cowork sessions collide on the Hermes GUI lock; `hermes-worker-queue` exists to run deterministic work without contending for that lock. Both are, underneath, ways of not polling for another agent's completion. A one-shot idle notice is the primitive those queues were approximating. The caveat matters and should stop anyone from over-reading it: this coordinates Claude Code sessions on a single machine, which is not the same object as Hermes-the-desktop-app or the launchd job queue, so it does not wire the existing swarm and nobody here has run it.

9. **Anthropic shipped a SECOND protocol — the Model Hardware Standard, a research-preview spec for AI agents to safely OPERATE PHYSICAL DEVICES, with device-declared safety limits, an auto-generated capability file, and MCP as one of three control mechanisms** ([www.anthropic.com](https://www.anthropic.com/news/model-hardware-standard-research-preview)) — Read off Anthropic's own newsroom post dated Aug 27, 2026, not a summary of it. MHS is "a shared specification for AI agents to safely operate physical devices," begun as a collaboration between Anthropic's Beneficial Deployments team and HHMI Janelia. What it actually specifies: a standardized driver; a primitive command set (read/write); standard-format discoverability so devices and agents find each other with no bespoke translator program between them; natural-language tags written into the driver for machine characteristics not discernible from code (the post's own example is a robot arm's weight, needed to manipulate it safely); an auto-generated reference file describing what a device can measure, what can be adjusted, and what safety limits will be enforced; and three control mechanisms — MCP, the command line, and code files — that "work together to enable orchestration across multiple devices via a single line of code." MHS is model-agnostic and reachable by any agent harness over standard protocols including MCP; it is explicitly NOT built on top of MCP. Case-study partners are Genentech, Carnegie Mellon, the University of Washington Baker and Pinglay labs, QuEra Computing, Tetsuwan Scientific and Janelia, with driver work from AWS, Tecan, QIAGEN, Doosan Robotics, Universal Robots, MBF Bioscience, Automata, Hugging Face's LeRobot and Raspberry Pi. The stated limits are unusually candid: Claude "learns about the physical world through text and images" so spatial reasoning stays limited and expert oversight is still required, MHS "doesn't yet work with hardware that lacks a programming interface," and Anthropic says "we have more work to do on the standard before we open-source it" with no date, quarter or year attached. **Applicable: YES** — YES, and it is the largest item in this corpus today. `Model Hardware Standard`, `MHS`, `Janelia`, `QuEra` and `Tetsuwan` all return zero matches across every file. The reason it lands here rather than filing as science news is that Brand 9 is a fabrication shop — routers, printers, laminators, cutters — and every MCP item this report has ever carried was about querying DATA. This one is about operating MACHINES, which is a different category of thing and the first of its kind logged. Honest scoping first, because the temptation to overread is obvious: this is a research preview for scientific research labs and advanced manufacturers, there is no sign-industry angle, and nobody here should apply for the waitlist. What transfers is three things. First, the architecture: a device declares its own capabilities and safety limits into a reference file, and the agent reads that file rather than being told what the device does. That is the third convergence in a single day on the same pattern — PTC's FeatureScript this morning, the skills library itself, and now a hardware spec — that the durable artifact is the unit of work and re-prompting is the thing being designed out. Second, the safety posture is the strongest external validation this week of the read-allow / write-approve boundary this report keeps arguing for: CMU blocked six artificially induced fault conditions before any device moved, and QuEra's stated principle — "an overly cautious agent is preferable to one that is not cautious enough" — comes from a team whose agent can fire a laser, which is a more expensive place to learn it than a deploy script. Third, for PRAGMA the pitch shifts again in two days: Monday's argument was that agents are arriving in construction data, and today the vendor published a specification for agents driving physical instruments. The QuEra numbers are the ones to say out loud in a room, because they are specific and verifiable: a bespoke script relocked roughly 58% of the time at about 150 seconds per attempt, and an overnight four-role agent loop took it to about 6 seconds and 96%, with a later blind test recovering 695 of 700 trials. Do not overclaim them — those are lab instruments under expert supervision, and Anthropic itself names the spatial-reasoning limit in the same post.

10. **The first LARGE-SCALE measurement of how agent configuration is ACTUALLY used — 2,853 GitHub repositories — finds Skills at 9.6% adoption on Claude Code, 85.5% of Skills carrying no scripts or resources at all, and ZERO repositories using subagent memory** ([arxiv.org](https://arxiv.org/pdf/2602.14690)) — arXiv 2602.14690v5, "Harness Engineering for Agentic AI Coding Tools: An Exploratory Study" (Galster, Mohsenimofidi, Lulla, Abubakar, Treude, Baltes), the extended version of an AIware '26 paper, working from a February 2026 data snapshot. Method: 37,249 filtered repositories narrowed to 32,564 classified as engineered projects, then file-path detection identifying 2,853 repositories that configure at least one agentic coding tool. It names eight configuration mechanisms — Context Files, Settings, Skills, Subagents, Commands, Hooks, Rules and MCP servers — and reports that only Context Files and Skills are supported by all five tools studied (Claude Code, Copilot, Cursor, Gemini, Codex) and that no single tool implements all eight. The headline numbers: Context Files appear in 2,586 of 2,853 repositories (90.6%) and are frequently the only mechanism present; within Claude Code repositories Context Files reach 94.3% while Skills reach 9.6% and Subagents 6.8%; and across Claude, Copilot, Cursor and Gemini no mechanism other than Context Files exceeds 20% adoption, the only exceptions being Cursor Rules and Gemini Settings. On Skills specifically: 601 Skills across 158 repositories, median 2 per repository, only 29 of 601 (4.8%) exceeding the 500-line SKILL.md recommendation, and 514 of 601 (85.5%) shipping no additional resources whatsoever — `references/` and `scripts/` are tied at 35 each. On Subagents: 450 across 131 repositories, median 2, and on Claude Code's persistent subagent memory the authors state flatly that they "did not find repositories that store such memory files." CLAUDE.md is the single most common file (1,640) but AGENTS.md receives the most incoming cross-references (353), with 301 of CLAUDE.md's 344 outgoing references pointing at it — which the authors read as "bottom-up convergence on AGENTS.md across tools, driven by developer practice rather than vendor mandate." **Applicable: YES** — YES, and it is the first item this corpus has ever carried that measures the thing The Construct IS rather than describing something it could adopt. `2602.14690`, `Galster`, `Treude`, `Baltes`, `2,853` and `Subagents` all return zero matches, and `AGENTS.md` has not appeared since 07-07. Read plainly, this stack is a statistical outlier, and that cuts both ways honestly. A library of roughly ninety skills sits far outside a median of two per repository — that is either the moat this report keeps calling it, or an unvalidated bet, and the paper is explicit that it does NOT measure whether richer configuration outperforms a plain context file. Nobody gets to cite this as proof the approach works. Two findings should change behaviour anyway. First, 85.5% of skills in the wild are pure prose with no executable resources, while the authors' own recommendation is that Skills earn their keep on recurring, well-defined workflows by bundling scripts and structured resources. The local skills that already ship real machinery — the MAXScript job-drop pattern, the headless repair channel, the render-verify gate — are exactly the shape the research endorses; the ones that are only prose are the audit list, and that audit is a concrete afternoon of work rather than a project. Second, the hierarchy recommendation is free and directly applicable: keep one shared core file and make tool-specific files thin adapters that reference it, because layered instruction files risk "redundant or conflicting instructions." This environment has a CLAUDE.md, a memory index under ~/.hermes/memories, and ninety-odd skill descriptions all asserting rules at the model simultaneously, and no one has ever checked them against each other for conflict — the paper's closing call for methods to detect exactly that is a gap this shop is living inside. Third and smallest: zero repositories anywhere use subagent memory, which makes it an industry-wide unused capability worth one bounded experiment, not a rollout. The authors' own caveats deserve to be carried with the numbers: open-source only, no enterprise data, a point-in-time snapshot with no churn analysis, and no performance measurement of any mechanism.

11. **Anthropic opened 10,000 Claude seats to scientists on the SAME DAY as the hardware standard — free standard seats, $15/month premium at 5x limits — and extended AI for Science credits beyond biology into every field** ([www.anthropic.com](https://www.anthropic.com/news/expanding-support-for-scientists)) — Announced Aug 27, 2026, published alongside the Model Hardware Standard preview. A new Claude team plan for scientists opens 10,000 seats for one year: standard seats free, premium seats carrying 5x usage limits at $15 per month. Eligibility is gated — a verification form, and principal-investigator-or-equivalent status at an academic or nonprofit research institution. Anthropic states it intends to extend the program well beyond the initial 10,000 seats over the coming months, and that the existing AI for Science credit program, which until now largely supported biological sciences, expands in scope to every scientific field. **Applicable: NO** — NO for Brand 9 and NO for PRAGMA as an opportunity — nobody in this shop is a principal investigator at a nonprofit research institution, the eligibility gate is explicit, and this is logged as ecosystem context rather than something to act on. `AI for Science` and `10,000 seats` both return zero corpus matches, so it is a real gap being closed, just not a useful one. It earns its line for two reasons. The pairing is the actual signal: on one day the same vendor published a specification for agents operating laboratory hardware AND a program that puts ten thousand laboratories on the product. That is not two announcements, it is distribution for the first one — a standard is worthless without adopters, and this is how the adopters get manufactured. Reading them separately would miss the strategy entirely, which is a small argument for scanning a newsroom index rather than chasing individual headlines. Second, $15/month for 5x usage limits is the lowest published Anthropic usage-tier price this report has recorded, and while the offer is unavailable here it is a usable anchor for what the vendor believes a subsidised power seat is worth — relevant to PRAGMA pricing conversations in the same way a competitor's discount sheet is relevant without being purchasable. The honest verdict: informative about vendor intent, actionable for no one here.

12. **Two COST fixes in 2.1.248 that BOTH earlier fires missed today — a prompt-cache miss roughly ONCE AN HOUR in long sessions caused by tool definitions re-rendering after an OAuth token refresh, and a `ScheduleWakeup` definition change blowing the cache on every `--resume`** ([code.claude.com](https://code.claude.com/docs/en/changelog)) — Read off the same canonical changelog both earlier fires used, but line by line through the bug-fix list rather than stopping at the headline entries. Two fixes land squarely on long-running scheduled work. First: a prompt-cache miss, and lost extended-thinking context, roughly once an hour in long sessions, caused by tool definitions being re-rendered after an OAuth token refresh. Second: the `ScheduleWakeup` tool definition changing between a session and its `--resume` when the account had entered usage overage, causing a full prompt-cache miss on the resumed session's first turn. Three smaller entries in the same release are also unlogged: MCP servers whose `headersHelper` supplies the Authorization header were falling into OAuth discovery on a 401 instead of re-running the helper and retrying the call as documented; an invalid `crossSessionInbound` value was being silently ignored and now warns on user settings or refuses on managed settings; and `/loop` self-paced dynamic mode plus the no-prompt autonomous default are now always available, including on Bedrock, Vertex and Foundry. Checks run in this window that returned nothing new: the changelog's newest entry is still 2.1.250 (Aug 28, bug fixes and reliability only), with no 2.1.249, 2.1.251 or beyond in existence; no frontier lab shipped a flagship, leaving Zhipu's GLM-5.3-Flash and Alibaba's Qwen3.8-Flash-Next on Aug 26 as the newest releases after this morning's correction; and a direct search for a signage, wayfinding or permitting MCP server again returned only industry marketing pages, so that standing negative holds for a fourth day. **Applicable: YES** — YES, and the once-an-hour cache miss is the most directly billable finding of the day. `ScheduleWakeup`, `crossSessionInbound` and `token refresh` all return zero corpus matches, and `prompt-cache` appears only inside cost-report.json — never in a Pulse entry. It matters here for a specific reason rather than a general one: this fleet's defining workload is the long unattended session — the Lens sweep, this Pulse scan, the answer-page checks, the ANS render loops — and a cache miss recurring hourly in exactly that workload is a quiet repeating bill that nobody would ever have attributed to an OAuth token refresh. Combined with `experimental.cacheTtl` in agent frontmatter from this morning's bullet 2, `ANTHROPIC_DEFAULT_MODEL` from yesterday, `/claude-api cost-optimize` from bullet 3, and the per-loop `/usage` panel, the fleet now has five independent cost levers logged across three days and has pulled exactly none of them. That has stopped being a research gap and become an execution gap, and it belongs on the same single task as the version-and-config check now open for four days — past 2.1.223, past 2.1.246, past 2.1.248, no wildcard before a Bash subcommand, and whether any machine sets `ANTHROPIC_BASE_URL`. The `headersHelper` fix deserves its own note because this session opened with sixteen MCP servers needing authentication and five failing to connect: a client that falls into OAuth discovery instead of retrying with the helper's header is a plausible contributor to exactly that noise, and it is now fixed upstream. The method lesson is the durable part. Both earlier fires today read this same canonical file and both took its headline entries; the two expensive fixes were sitting in the same document, further down, in the list nobody reads. Reading the canonical source instead of a summary was the right correction and it is not sufficient — reading all of it is the actual discipline.

**PRAXIS lesson:** Three fires read the same changelog today and the third one found the most billable item in it — a prompt-cache miss roughly once an hour in long sessions, caused by tool definitions re-rendering after an OAuth token refresh — sitting in the bug-fix list below the headline entries the first two stopped at. Reading the canonical source instead of a summary was the right rule this morning and it is not enough; the discipline is reading all of it, because vendors file the expensive fixes where the announcements are not. The window's real news is that Anthropic shipped a SECOND protocol: the Model Hardware Standard puts agents in control of physical instruments through device-declared safety limits and an auto-generated capability file, which is the third convergence in a single day — after PTC's FeatureScript and the skills library itself — on authoring a durable declarative artifact instead of re-prompting. And the first large-scale measurement of that practice landed the same day: across 2,853 repositories Skills reach under 10% adoption on Claude Code, 85.5% carry no scripts at all, and no repository anywhere uses subagent memory, which makes this stack a genuine outlier whose bet is real but unvalidated — the paper is explicit that it does not measure whether the bet pays.
