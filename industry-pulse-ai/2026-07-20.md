# Industry Pulse — AI / Agent Ecosystem · 2026-07-20

Daily scan for The Construct. Source URL · one-line summary · applicable to The Construct.

1. **Claude Code — `/verify` and `/code-review` are now invoke-only, plus a broad safety pass and live MCP connector data for published artifacts** ([releasebot.io](https://releasebot.io/updates/anthropic/claude-code)) — the freshest release (first seen Jul 19) stops Claude from auto-running the `/verify` and `/code-review` skills unless invoked directly, ships tighter permission checks and safer Bash/PowerShell handling with the EndConversation tool and progress heartbeats for long-running tasks, and — most relevant to us — adds live MCP connector data to published artifacts so an artifact can pull fresh connector data each time it's opened. **Applicable: YES** — the invoke-only change touches how our scan agents run skills (confirmable first-hand in this session), and live-connector artifacts are a direct fit for The Construct dashboard surface, where a published view should refresh from `brand-insights/latest.json` rather than freeze at publish time.

2. **MCP spec 2026-07-28 is now 8 days out — final adds MCP Apps (server-rendered UIs) and a Tasks extension for long-running work on top of the stateless core** ([blog.modelcontextprotocol.io](https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/)) — beyond the breaking stateless rewrite already flagged (initialize/initialized handshake and Mcp-Session-Id gone, Mcp-Method routing headers, ttlMs/cacheScope on list/read, OAuth/OpenID auth, -32002→-32602), the final spec formally lands MCP Apps for server-rendered UI inside the client and a Tasks extension for long-running jobs. **Applicable: YES** — every MCP server The Construct runs is 8 days from a breaking change; MCP Apps is a genuinely new capability (an agent-callable server can now render its own UI), and the Tasks extension maps to our long-running scan jobs. Run the one `/doctor` pass that yields both the unused-server list and the Jul-28 migration inventory before the deadline, not after.

3. **arXiv: "The Orchestration of Multi-Agent Systems: Architectures, Protocols, and Enterprise Adoption" — unifies MCP tool access with the Agent2Agent protocol for peer coordination, delegation, and policy enforcement** ([arxiv.org/2601.13671](https://arxiv.org/abs/2601.13671)) — formalizes a single architectural frame where agents reach tools via MCP and coordinate with each other via A2A, with explicit delegation and policy-enforcement layers — the current live research vein (AdaptOrch, Uno-Orchestra, DyTopo all in the same cluster). **Applicable: MAYBE** — vocabulary and a governance frame for the fleet: The Construct already routes skills per task via MCP, but A2A (agent-to-agent) is a protocol dimension we don't yet use and should watch as the installed-skills registry grows. A framework to borrow against CLAUDE.md governance, not a tool to install today.

**PRAXIS lesson:** Quiet primary-release window — no new frontier model in the last 24h (GPT-5.6, Sonnet 5, Grok 4.5, Kimi K3 all off-window), so the load-bearing item is a deadline, not a launch: the Jul-28 MCP breaking change is now 8 days out and the final spec adds MCP Apps + Tasks on top of the stateless rewrite. On a slow day the disciplined move is to spend it on the migration inventory you already owe, and to note that "live MCP connector data for published artifacts" is the exact mechanism that would keep the V4 dashboard fresh from `latest.json` instead of frozen at publish.
