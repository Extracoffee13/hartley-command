# Industry Pulse — AI / Agent Ecosystem · 2026-07-19

Daily scan for The Construct. Source URL · one-line summary · applicable to The Construct.

_Second fire, 10:01 ET — the 06:01 brief was re-checked against fresh searches and refreshed in place. Item 1 upgraded to the newest Claude Code changelog (fresher than the Jul-18 entry the morning cited); items 2–4 preserved, MCP Jul-28 deadline still 9 days out._

1. **Claude Code — latest changelog adds the `EndConversation` tool, progress heartbeats for long-running tasks, and makes `/verify` + `/code-review` invoke-only, alongside tighter permission scoping** ([code.claude.com/docs changelog](https://code.claude.com/docs/en/changelog) · [releasebot.io](https://releasebot.io/updates/anthropic/claude-code)) — the newest release (checked this fire, fresher than the Jul-18 `/fork`-background entry the morning cited) is a broad stability-and-safety pass: it adds an `EndConversation` tool (no-op inside background forks), a periodic progress heartbeat for long-running tool calls that previously went silent, tighter permission checks, and safer Bash/PowerShell handling. Two changes matter most for a fleet: `/verify` and `/code-review` now run **only when invoked directly** (Claude no longer fires them on its own), and the single-segment `dir/**` allow-rule bug is fixed so `Edit(src/**)` no longer auto-approves writes to nested `dir/` directories anywhere in the tree. **Applicable: YES** — the invoke-only change for `/verify` and `/code-review` is confirmed live in this very session's tooling (good provenance, not a vendor claim), and the tightened `dir/**` scoping is a real safety improvement for our fan-out subagent jobs; progress heartbeats also close the "long job went silent" gap on scans like this one. Audit our permission allow-rules against the new scoping before the next big fleet run.

2. **Gemini Omni Flash lands in Google Vids (Jul 17) — conversational video editing + personal avatars from a text prompt** ([workspace.google.com blog](https://workspace.google.com/blog/product-announcements/introducing-gemini-omni-flash-in-google-vids) · [thenextweb.com](https://thenextweb.com/news/google-gemini-omni-flash-video-model-io-2026)) — Google put Omni Flash inside Google Vids on Jul 17: edit existing video with plain-text prompts and generate new clips with an avatar that looks and sounds like you. The underlying model (developer API since Jun 30) takes any mix of text/image/audio/video in and produces physics-aware video, at $0.10 per second of output, with conversational editing as its differentiator versus every other API video model. **Applicable: MAYBE** — this is squarely on Brand 9 Signs' surface (short-form video for signage clients, social cuts) and on Vector's remit in The Construct, but we already route generative video/image through the Higgsfield MCP fleet. Worth a single bench: does conversational text-prompt editing at $0.10/sec beat our current pipeline for a real B9S social cut, or is it a second vendor we don't need? Test, don't adopt on the strength of a launch post. (New this fire, not previously flagged: Revid.ai shipped a video-production MCP Jul 16 — script-to-video, `schedule_publish`/`publish_now` — a closer-to-B9S bench candidate than Omni Flash if we want an agent-callable pipeline; off-window but parked for the video bench.)

3. **MCP specification `2026-07-28` goes final in 9 days — the largest breaking revision since launch (carried)** ([blog.modelcontextprotocol.io](https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/) · [workos.com](https://workos.com/blog/mcp-2026-spec-agent-authentication)) — the maintainers' RC stands: the core goes stateless (the `initialize`/`initialized` handshake and `Mcp-Session-Id` header are removed; any request can land on any instance), Streamable HTTP now requires `Mcp-Method` and `Mcp-Name` routing headers, list/read results carry `ttlMs` and `cacheScope`, authorization aligns to OAuth/OpenID Connect, and the missing-resource error moves from custom `-32002` to standard `-32602`. SDK betas (Python v2 renames FastMCP→MCPServer; TypeScript v2 is ESM-only and split into `@modelcontextprotocol/server` + `/client`; Go v1.7.0-pre.1; C# 2.0.0-preview.1) are out now. **Applicable: YES** — every MCP server The Construct runs is on the far side of a breaking change in 9 days. Two exposures remain live: any code matching the literal `-32002`, and any skill assuming session-scoped state the transport will no longer carry. The `/doctor` pass flagged this week is the vehicle — produce the unused-server list and the Jul-28 migration inventory in the same sitting, and do it before the deadline, not on Jul 29.

4. **arXiv "Context Engineering: From Prompts to Corporate Multi-Agent Architecture" — five context-quality criteria for a governed fleet** ([arxiv.org/abs/2603.09619](https://arxiv.org/abs/2603.09619)) — the paper reframes context management for enterprise agent fleets around five quality criteria — relevance, sufficiency, isolation, economy, provenance — and introduces "intent engineering": encoding organizational goals, values, and trade-off hierarchies into the agent infrastructure itself rather than into individual prompts. **Applicable: MAYBE** — this is a clean vocabulary for what The Construct already does ad hoc across its skill/MCP fleet, and the "economy" and "provenance" criteria map exactly onto the two things this scan keeps relearning (context cost of unused servers; never letting an unconfirmed detail age into fact). A framework to borrow for the installed-skills registry and the memory system, not a tool to install — read it against our CLAUDE.md governance and the `/doctor` output.

_Seen and not flagged this fire: OpenAI GPT-5.6 (Luna/Terra/Sol) and Google Gemini 3.5 Flash / Grok 4.5 — real frontier releases but off-window (early–mid July) and already standing context, not 24h news. Kimi K3 (Moonshot, Jul 16, open weights promised Jul 27) — off-window, tracked for the open-weight drop. July agent-funding roundup (Perplexity $60M ext at $3.5B; sector ~$1.8B across 12+ deals, avg $150M, 62% Series B+) — market context, nothing directly actionable for The Construct / B9S / PRAGMA. Revid.ai video MCP (Jul 16) — noted under item 2._

**PRAXIS lesson:** The clean second-fire move is to let the freshest primary source overwrite the morning's placeholder: the 06:01 brief cited the Jul-18 `/fork` changelog, but the newest Claude Code release is a safety pass whose most load-bearing change — `/verify` and `/code-review` now invoke-only — is confirmable live in this session's own tooling, so it replaces a vendor-page claim with a first-hand fact. Bank what you can verify first-hand, and still run one `/doctor` pass that does double duty: the unused-MCP-server list and the Jul-28 migration inventory, before the deadline.
