# Industry Pulse — AI / Agent Ecosystem · 2026-09-21

Daily scan for The Construct. Source URL · one-line summary · applicable to The Construct. **Day twenty-one, FIRST fire, ~06:00 ET.** Run under the 14 Sep claim-grep (every candidate grepped against all 83 archive files), the 15 Sep intake rule (read the dateline at the page), the 16 Sep dated-index rule, the 17 Sep vendor-self-index rule, the 17 Sep diff-not-paste rule, the 18 Sep lab-research-blog lane, the 18 Sep vendor-newswire lane, the 19 Sep T-48h re-scan rule, the 19 Sep item (q) own-product-surfaces lane, the 19 Sep item (r) eligibility rule, the 20 Sep item (s) security/disclosure lane, the 20 Sep item (u) fail-open lane and — **for the first time — item (v), the standards-body lane written yesterday at 14:00.** Method note: the Claude Code changelog primary was fetched and read for the **seventh consecutive read**; the weekly agent index was re-fetched and its Sunday 20 Sep block read in full. **One dateline correction declared** (Safari 27, below). **This fire's lead is about this report's own intake, not about the industry** — and unlike yesterday's archive miss, it is not a miss, it is a **near-publication of something that does not exist**, caught by one rule.

1. **LEAD — THE SEARCH TOOL THIS REPORT RUNS ON FABRICATED A CLAUDE CODE RELEASE THAT DOES NOT EXIST, GAVE IT A VERSION NUMBER, A DATE AND NINE PLAUSIBLE CHANGELOG ENTRIES — AND THE ONLY THING THAT CAUGHT IT WAS THE RULE THAT SAYS FETCH THE PRIMARY** ([Claude Code changelog, primary, fetched and read 06:00 ET — latest entry is **2.1.278, September 19, 2026**](https://code.claude.com/docs/en/changelog) · [second independent search, which correctly reports "the latest version is 2.1.278, not 2.1.279"](https://claudelog.com/claude-code-changelog/)) — the first query of this fire returned a confident, structured summary of **"Claude Code version 2.1.279 from September 2026,"** with four bug fixes, three new features and one change, formatted exactly like a real release. **There is no 2.1.279.** Every one of the nine entries is real text, and every one of them belongs to a **different, earlier release**: the `advisor_20260301` proxy fix is **2.1.276** (18 Sep); the gateway signed-in-account row, the `ctrl+enter` send-now key and the claude.ai skills/plugins sync are **2.1.275** (17 Sep); the language-server diagnostics slowdown and the v2-MCP-client default are **2.1.274** (17 Sep); the duplicated background command after compaction is **2.1.271** (14 Sep). **A synthesis layer took four real releases, merged them, and invented a container with a version number one higher than the real head.** Archive grep across 83 files: `send-now` ZERO, `ctrl+enter` ZERO, `v2 MCP client` ZERO, `MCP_SDK_GENERATION` ZERO, `MCP_PROTOCOL_NEGOTIATION` ZERO, `gateway sign-in` ZERO — **and `2.1.279` appears twice, in yesterday's two fires, both times in the sentence "no 2.1.279."** Yesterday this report wrote that string as a negative finding; today the same string came back as a positive one, from a tool, with detail attached. **Applicable: YES — and it is the first finding this report has ever produced about its own instrument rather than its own attention.** **(i) The failure mode is new and it is the dangerous direction.** Every previous method lesson here has been about *missing* something — item (s) missed OWASP by nineteen days, item (u) walked past a fail-open line five times. This is the opposite: **the pipeline produced a fully-formed artifact that was never real, and it was more detailed and more confident than most of the true items in this file.** An omission is discovered eventually. A fabrication that survives one fire becomes an archive entry, and every later fire greps against it. **(ii) The rule that caught it is one rule, and it is cheap.** The 15 Sep intake rule — *read the dateline at the page* — required fetching `code.claude.com/docs/en/changelog` rather than trusting the summary. That single fetch is what stood between this and a bulleted non-existent release. **The rule is now load-bearing, not hygienic.** **(iii) NEW ITEM (w): a search-tool synthesis is a claim, not a source.** Any version number, date, figure or quoted line that reaches this report through a search summary rather than a fetched primary is **unconfirmed until fetched**, and must be labelled as such if it appears at all. **(iv) And the honest asymmetry: item (q) paid out again, and it paid out against us.** Our own product surfaces lane is now three-for-three — connectivity (19 Sep), cost (20 Sep), and today **provenance.**

2. **ITEM (c) WAS FINALLY RUN — AFTER SIX CARRIED FIRES AND TWENTY-ONE DAYS — AND IT RETURNS A NUMBER: THE CONSTRUCT HAS 125 SKILLS AND *NOT ONE OF THEM* DECLARES A TOOL RESTRICTION, SO EVERY SKILL IN THE CORPUS CAN INVOKE EVERY TOOL** ([the finding that triggered it: `allowed-tools` only *pre-approves* tools and is **not a guard**, while `disallowed-tools` is the real hard denial](https://github.com/garygentry/feature-forge/pull/294) · [skill-forge — validate, pressure-test and release-gate SKILL.md packages, with network default-deny, credentials absent, source read-only, scratch-only writes](https://github.com/zztimur/skill-forge) · [arXiv 2605.28000, *Tool Forge: A Validation-Carrying Toolchain for Governed Agentic Execution*](https://arxiv.org/html/2605.28000)) — **archive grep across 83 files: `allowed-tools` ZERO, `disallowed-tools` ZERO, `disable-model-invocation` ZERO, `skill-forge` ZERO, `Tool Forge` ZERO, `default-deny` ZERO, `tool-registration` ZERO.** **Measured this fire, not asserted:** 125 `SKILL.md` files in the loaded corpus; frontmatter keys actually used across all 125 are `name:` (125), `description:` (125), `version:` (5), `license:` (5), `sources:` (3), `status:` (2), `spec:` (2), `disable-model-invocation:` (**1**). **`allowed-tools`: zero. `disallowed-tools`: zero.** **Applicable: YES, and it closes the oldest open item on the list by executing it rather than by reading about it.** **(i) The trigger is a fail-open finding about a field we do not use, which turns out to be the *better* news.** The external claim is that `allowed-tools` is a soft pre-approval and only `disallowed-tools` denies — a distinction that would have bitten us if we had written `allowed-tools` everywhere and believed it was a guard. **We did not write it anywhere, so we never held the false belief — we simply have no restriction at all, which is at least honest.** **(ii) The number is the point, and it is the first hard number this report has ever produced about our own estate.** Twenty-one days of "audit the skill corpus" produced nothing; one grep produced *125 / 0 / 0 / 1*. **The item was never blocked on information and it was never blocked on tooling — it was blocked on nobody running it, exactly as (p) has claimed for five consecutive fires.** **(iii) What to do with it is small and specific.** Not "restrict all 125." The credentialed ones — anything that reaches DreamHost SSH, GSC, GA4, GitHub, the Hermes deploy key — are the ones where a `disallowed-tools` line is worth writing, and that is a short list Bobby already knows by name. **(iv) Item (v) paid out on day one, the fourth lane in a row to do so** ((s) day one, (q) day one, (u) day four hours): the standards/validation-tooling category added yesterday produced `skill-forge` and the Tool Forge paper, neither of which any vendor lane would have surfaced.

3. **SAFARI 27 SHIPPED A NATIVE MCP SERVER — A BROWSER ON BOBBY'S OWN MACS IS NOW AN AGENT-DRIVABLE SURFACE — AND TWO SECONDARY OUTLETS MAKE FLATLY OPPOSITE CLAIMS ABOUT WHETHER IT CAN BE TURNED OFF, WHICH IS BULLET 1'S LESSON ARRIVING TWICE IN ONE FIRE** ([WebKit, the primary — *Introducing the Safari MCP server for web developers*](https://webkit.org/blog/18136/introducing-the-safari-mcp-server-for-web-developers/) · [9to5Mac, 17 Sep](https://9to5mac.com/2026/09/17/webkit-blog-breaks-down-whats-new-with-safari-27-for-developers-including-mcp-support/) · [the aggregator entry that surfaced it, filed under 20 Sep](https://aiagentstore.ai/ai-agent-news/this-week)) — **DATELINE CORRECTION DECLARED:** the weekly index files this under **Sunday 20 September**; the WebKit primary and the 9to5Mac report are **17 September**, Safari 27.0's own ship date, and 17 Sep is the date used here. **Archive grep across 83 files: `Safari 27` ZERO, `local MCP server` ZERO, `WebKit` ZERO, `remote automation` ZERO, `automation window` ZERO; `Apple` appears in 2 files, neither about this.** Read, at the primary: an MCP-compatible agent can open a live Safari tab, inspect the DOM, read console errors, watch network requests and take screenshots; it runs **entirely locally and makes no network calls of its own**; it uses a **dedicated automation window isolated from the user's session, with no access to cookies, passwords, AutoFill or history**; and it requires a **two-step manual opt-in** — *Show features for web developers* under Advanced, then *Allow remote automation and external agents* under Developer. **Applicable: YES, on two separate axes.** **(i) The operational one.** Item (l) has carried "audit the Chrome extensions" for eight days on the theory that the browser is the least-governed surface we run. **A second browser on the same machines just grew an agent hook**, and the standing rule in `CLAUDE.md` — *never open a new Chrome window, always attach to the already-open Chrome* — exists precisely because browser automation on Bobby's real machine has bitten us before. The check is two settings panes and takes a minute. **(ii) The method one, and it is the sharper half.** One outlet headlines it as **"Apple gave enterprises no way to turn it off"**; another describes an explicit two-step opt-in and an isolated window with no credential access. **Both cannot be true, and the primary supports the second.** That is the second time in one fire that a confident secondary account diverged from the primary on a *control* — bullet 1 invented a release, this one inverted a default. **New item (w) is therefore not about one flaky query; it is about a class.**

4. **STANDING ACTIONS — DAY TWENTY-ONE, FIRST FIRE; CLAUDE CODE LANE READ AT THE PRIMARY: STILL 2.1.278 (19 SEP), NO 2.1.279, SEVENTH CONSECUTIVE READ, AND SEE BULLET 1 FOR WHY THAT SENTENCE MATTERED TODAY** ([Claude Code changelog, primary](https://code.claude.com/docs/en/changelog)) — **(a) incident log** — unchanged in specification, **still not started**; it has been fully specified since 14:00 yesterday (Anthropic's ratio, GitLab's attribution schema, ACS's fail-safe clause, Pizza Bot's inbox UI) and the correct action remains *build the post-hoc reader, stop reading about it*. One addition worth exactly one line: Swarms' 19 Sep changelog ships **per-completion permalinks with per-task cost and failure tracing** — the attribution UI, NAMED NOT BULLETED, aggregator-only, `Swarms` in 1 archive file, `per-completion` ZERO. **(b) managed-MCP controls** — **CHANGED**: bullet 3 adds a *platform* MCP surface (Safari) to a list that until now held only servers and frameworks. **(c) skill-corpus audit** — **RUN. CLOSED AS AN AUDIT, OPENED AS A FIX.** Result: 125 skills, 0 tool restrictions, 1 `disable-model-invocation`. New sub-item: write `disallowed-tools` on the credentialed skills only. **Six carried fires ended by one grep.** **(f) creative-ops MCP behind `graphic-designer`** — unchanged, 70 days, **seventh carried fire**. **(g) dated-index source list** — **CHANGED**: add `webkit.org/blog` (a platform vendor whose release notes now carry agent surfaces) and `arxiv.org` validation-tooling queries under item (v). **(h) scout set** — unchanged. **(i) paid-lane position** — **DEADLINE IN TWO DAYS (23 Sep).** **(j) one gate with enforcement power** — unchanged; ACS remains the yardstick, and bullet 2 supplies the *first thing to measure*: 0 of 125 skills declare a tool boundary. **(k) credential grep** — **STILL NOT RUN, 23rd day**, and bullet 2 makes it worse rather than better: we now know 125 skills can call any tool, and we still do not know which of them hold or reach a credential. **This is no longer the highest-value unexecuted item — it is the *only* one that still has no number attached.** **(l) Chrome extensions** — **CHANGED, eighth day**: the scope grew a browser (bullet 3). **(m) `syncClaudeAiSkills` / `syncClaudeAiPlugins`** — **CHANGED, first movement since it was written**: the primary (2.1.275, 17 Sep) says claude.ai-enabled skills and plugins now **sync into terminal sessions signed in with that account**, opt out with `syncClaudeAiSkills: false` / `syncClaudeAiPlugins: false`. Read against bullet 2 this is not a convenience, it is a **remote write path into the 125-skill corpus we just measured**. **(n)** unchanged. **(o) WSO2 Agent Manager** — unchanged, sixth carried fire. **(p) monitor our own standing list** — sixth test, and **the first one that passes**: (c) moved from *carried* to *executed*, which is the first time in six tests that an item left the never-moves class. The mechanism was not a new source; it was a twenty-minute grep. **(q) our own product surfaces** — **three-for-three: connectivity, cost, now provenance** (bullet 1). **(r) check our own eligibility** — fifth application, correct negative on Swarms (aggregator-only, not our stack). **(s) security/disclosure lane** — day three, quiet. **(t) `claude --version` + plugin/MCP inventory** — **CHANGED**: add the Safari MCP setting to the inventory, and add `syncClaudeAiSkills` / `syncClaudeAiPlugins` as the two values that decide whether the local corpus is authoritative. **(u) fail-open lane** — day two, **paid out again** via the `allowed-tools`-is-not-a-guard finding (bullet 2). **(v) standards bodies and validation tooling** — **day one, VALIDATED ON DAY ONE**, the fourth consecutive lane to do so; it produced `skill-forge` and arXiv 2605.28000, and neither would have come from a vendor. **NEW (w): a search-tool synthesis is a claim, not a source.** Version numbers, dates, figures and quoted lines are unconfirmed until fetched at the primary; a summary that cannot be traced to a fetched page is labelled unconfirmed or omitted. **CONFIG (unwritten):** `omitClaudeMd` (18th naming); `maxEffortLevel`; `--system-prompt-snapshot off`; `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS`; per-command `allowed_domains`; `CLAUDE_CODE_GATEWAY_HINT_HEADERS=1`; `CLAUDE_CODE_MCP_STARTUP_WAIT_MS`; `CLAUDE_GATEWAY_PROXY_IS_EGRESS_BOUNDARY`; the gateway `headers:` map; `strictKnownMarketplaces` / `blockedMarketplaces`; **NEWLY ADDED:** `syncClaudeAiSkills`, `syncClaudeAiPlugins`, `MCP_SDK_GENERATION`, `MCP_PROTOCOL_NEGOTIATION`, and the two Safari panes. **RE-TEST:** `reconcile-before-building` across a compaction boundary — seventh instance in seven days, still not scheduled. **DEADLINES:** ChatGPT Ads international 23 Sep, Sonar 27 Sep, Claude for Financial Advisors credit end-September, Docusign MCP GA 30 Sep, GitLab per-user/per-group agent metering 19 Oct, Cohesity year-end, client-cert 17 Nov, DCR removal after summer 2027. **WATCH:** FRONTIER Act / Thune-Cruz-Klobuchar; Trump AI czar; Anthropic EFS "this fall"; Anthropic reported targeting a November IPO; ACS v0.1 → v1.0; AutoGen consolidation into Microsoft Agent Framework (Traversaal, 19 Sep, aggregator-only, `Traversaal` ZERO, `AutoGen` in 1 file — a migration-risk note with no hook for us since we do not run AutoGen). **KILLED ON PRIOR COVERAGE:** OWASP ACS, GitLab 19.4, Claude Code 2.1.278's billing change (all 20 Sep); Anthropic 30,000-agent oversight, Microsoft Agent Framework 1.19.0, HubSpot UNBOUND/Aviator/Agent Hub, Plugin4Shell, AgentBeam, Alation AIOS and the Exaforce/Eve/Cohesity kill-switch wave, Salesforce Agentforce/AIforce/Live Nation, WSO2 Agent Manager, Google Files/Credentials APIs and Labs CC, Meta Muse for Mac, Instinct Concierge, UN Data Commons, Baseten/HF/Goodfire, OpenAI compaction summaries, Cloudflare MCP v2, AWS Pizza Bot, CSA RubyGems, Abacus.AI Smaug. **KILLED ON PROVENANCE (new category, from bullet 1's rule):** the "Forge vnext changelog, 19 Sep" item in the weekly index — reported as shipping `forge skills validate` and default-deny tool guidance, but **no primary could be identified** (searches return Atlassian Forge, IBM mcp-context-forge and three unrelated projects); the *class* of finding was real and reached bullet 2 by a different, fetchable route, and the aggregator item itself is **not bulleted**. **REPORTED HONESTLY RATHER THAN EMPTY:** **frontier models** — nothing datelined 20 or 21 Sep on a sixth independent check; index unchanged. **MCP servers** — one, and it is a *browser* (bullet 3); the lane has now paid in a standard, a control and a platform surface on three consecutive fires rather than in servers. **Funding** — nothing in window; Crusoe's $3.9B Series F is 17 Sep and already outside, `Crusoe` in 1 archive file. **arXiv** — nothing datelined 20 or 21 Sep; 2605.28000 is May and is cited as a *method* source under bullet 2, declared, not presented as new. **Applicable: YES** — one item executed and closed, five changed, one new ((w)), one validated on day one ((v)), one paid out against us ((q)), and the list passed its own test for the first time in six attempts.

**PRAXIS lesson:** **FOR TWENTY DAYS THE FAILURE MODE OF THIS REPORT WAS MISSING THINGS. TODAY IT ALMOST PUBLISHED ONE THAT DOES NOT EXIST — AND THE DIFFERENCE MATTERS, BECAUSE AN OMISSION GETS FOUND LATER AND A FABRICATION BECOMES THE ARCHIVE THAT EVERY LATER FIRE GREPS AGAINST.** The first query of this fire returned "Claude Code 2.1.279," dated, with nine changelog entries. Every line of it was real text lifted from 2.1.271, 2.1.274, 2.1.275 and 2.1.276, reassembled under a version number one higher than the actual head. It was more specific and more confident than most of the true items in this file. **The only thing that stopped it was the 15 Sep intake rule — fetch the primary, read the dateline at the page — which until this morning looked like hygiene and is now the load-bearing wall.** That is new item (w): *a search-tool synthesis is a claim, not a source.* And it arrived twice in one fire, because bullet 3's Safari finding has one outlet insisting Apple shipped an agent hook enterprises cannot disable and the WebKit primary describing a two-step opt-in into an isolated window with no access to cookies, passwords or history. **Two confident secondaries, two inverted controls, one morning.** **SECOND, AND THIS IS THE ONE THAT ACTUALLY CHANGED SOMETHING: ITEM (c) GOT RUN, AND THE ANSWER IS 125 / 0 / 0 / 1.** One hundred and twenty-five skills in the corpus. Zero declare `allowed-tools`. Zero declare `disallowed-tools`. One declares `disable-model-invocation`. **Every skill The Construct loads can call every tool The Construct has**, including the ones that reach the DreamHost deploy key, GSC, GA4 and GitHub. For six consecutive fires this report wrote that (c) was "twenty minutes, three named files, still not run," and yesterday's lesson said flatly that this system converts insight into prose at a high rate and into changed behaviour at approximately zero. **Today it ran, and it took one grep, and it produced the first hard number this report has ever published about our own estate.** The follow-on is deliberately small — not "restrict all 125," but write one `disallowed-tools` line on the handful of credentialed skills Bobby can name from memory. **THIRD, THE UNCOMFORTABLE PAIRING, AND IT IS WHY (k) IS NOW THE ONLY ITEM LEFT WITHOUT A NUMBER.** Bullet 2 tells us every skill can call every tool. Item (k) — the credential grep, twenty-three days unrun — would tell us which of those skills can *reach a secret*. We now hold one half of a two-part fact, and the half we hold is the harmless one. **And item (m) supplies the twist: the claude.ai account's enabled skills and plugins now sync down into terminal sessions by default (2.1.275, opt out with `syncClaudeAiSkills: false`), which means the 125-file corpus we just measured is not purely local and can change from a surface nobody in this report has ever audited.** Measure the estate, then measure what the estate can reach, then decide what may write to it — in that order, and none of those three steps needs another source.
